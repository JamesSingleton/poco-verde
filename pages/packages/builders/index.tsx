import { Layout } from '@components/common';

const BuilderPackages = () => (
  <div>
    <h1>Builder Packages Index</h1>
  </div>
);

BuilderPackages.Layout = Layout;

export default BuilderPackages;
